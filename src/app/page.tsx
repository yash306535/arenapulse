/** Landing route — welcome, role switcher, and the feature directory. */
import { HomeScreen } from "@/components/features/home-screen";

export default function HomePage(): React.JSX.Element {
  return <HomeScreen />;
}
