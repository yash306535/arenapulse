/**
 * Test-only stub for the `server-only` package (aliased in vitest.config.ts).
 * Production builds keep the real guard, which prevents server modules from
 * ever being bundled into client components.
 */
export {};
